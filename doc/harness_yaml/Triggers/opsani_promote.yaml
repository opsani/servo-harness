harnessApiVersion: '1.0'
type: TRIGGER
executionName: Opsani Promotion
executionType: Pipeline
triggerCondition:
- type: WEBHOOK
workflowVariables:
- name: mem
  value: ${workflow.variables.mem}
- name: cpu
  value: ${workflow.variables.cpu}
